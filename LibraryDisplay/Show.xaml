<Page
    x:Class="LibraryDisplay.Show"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LibraryDisplay"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="mainGridShow">
        <Grid.RowDefinitions>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>

            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="tblShow" Height="950" Padding="7" Grid.Row="0" Grid.Column="1" FontSize="12" Width="700" HorizontalTextAlignment="Left"/>

        <Grid x:Name="GridShow2" Grid.Row="0" Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Button x:Name="MainFromShow" Content="Main" Grid.Row="1" Grid.Column="0" Click="toTheMainFromShow_Click"   />
            <Button x:Name="btnShow" Content="Show"  Grid.Row="1" Grid.Column="1" Click="btnShow_Click"/>
        </Grid>
    </Grid>
</Page>
